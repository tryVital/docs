<Note>
For Android _via_ Expo and React Native, the Health SDK API assumes Health Connect by default. To target Samsung Health, you must
pass `"samsung_health"` to the `provider` argument when calling the API.
</Note>
