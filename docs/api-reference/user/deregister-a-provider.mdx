---
title: "Deregister a Provider"
openapi: "DELETE /v2/user/{user_id}/{provider}"
---

<RequestExample>

```bash Shell
curl --request DELETE \
     --url {{BASE_URL}}/v2/user/{user_id}/provider \
     --header 'Accept: application/json' \
     --header 'x-vital-api-key: <API_KEY>'
```

```js JavaScript
const user = await client.User.deregisterProvider(user_id);
```

```python Python
user = client.User.deregister_provider(user_id)
```

</RequestExample>

<ResponseExample>

```json Example
{
  "success": true
}
```

</ResponseExample>
