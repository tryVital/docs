---
title: "Mindfulness Minutes"
openapi: "GET /v2/timeseries/{user_id}/mindfulness_minutes"
---

<RequestExample>

```bash cURL
curl --request GET \
     --url {{BASE_URL}}/v2/timeseries/{user_id}/mindfulness_minutes?start_date={{START_DATE}}&end_date={{END_DATE}} \
     --header 'Accept: application/json' \
     --header 'x-vital-api-key: <API_KEY>'
```

```python Python
from vital.client import Vital
from vital.environment import VitalEnvironment

client = Vital(
  api_key="YOUR_API_KEY",
  environment=VitalEnvironment.SANDBOX
)

data = client.vitals.mindfulness_minutes(
    user_id=*,
    start_date="2022-05-01",
    end_date="2022-06-01"
)
```

```javascript Node
import { VitalClient, VitalEnvironment } from '@tryvital/vital-node';
import { VitalsMindfulnessMinutesRequest } from '@tryvital/vital-node/api/resources/vitals';

const client = new VitalClient({
    apiKey: '<my_api_key>',
    environment: VitalEnvironment.Sandbox,
});

const request: VitalsMindfulnessMinutesRequest = {
    startDate: "2022-05-01",
    endDate: "2022-06-01"
}

const data = await client.vitals.mindfulnessMinutes(
    '<my_user_id>',
    request
);
```

```java Java
import com.vital.api.Vital;
import com.vital.api.core.Environment;
import com.vital.api.resources.vitals.requests.VitalsMindfulnessMinutesRequest;

Vital vital = Vital.builder()
        .apiKey("YOUR_API_KEY")
        .environment(Environment.SANDBOX)
        .build();

VitalsMindfulnessMinutesRequest request = VitalsMindfulnessMinutesRequest.builder()
        .startDate("2022-05-01")
        .endDate("2022-06-01")
        .build();

var data = vital.vitals().mindfulnessMinutes("<user_id>", request);
```

```go Go
import (
  "context"

  vital "github.com/tryVital/vital-go"
  vitalclient "github.com/tryVital/vital-go/client"
)

client := vitalclient.NewClient(
  vitalclient.WithApiKey("<YOUR_API_KEY>"),
  vitalclient.WithBaseURL(vital.Environments.Sandbox),
)

EndDate := "2022-06-01"
request := &vital.VitalsMindfulnessMinutesRequest{
  StartDate: "2022-05-01",
  EndDate:   &EndDate,
}

response, err := client.Vitals.MindfulnessMinutes(context.TODO(), "*", request)
if err != nil {
  return err
}
fmt.Printf("Received data %s\n", response)
```

</RequestExample>

<ResponseExample>

```json Example
[
  {
    "id": null,
    "timestamp": "2023-01-19T13:50:31+00:00",
    "start": "2023-01-19T13:50:31+00:00",
    "end": "2023-01-19T13:51:31+00:00",
    "value": 60.0,
    "type": null,
    "unit": "min"
  }
]
```

</ResponseExample>
