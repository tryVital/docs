---
title: "Hypnogram"
openapi: "GET /v2/timeseries/{user_id}/hypnogram"
---

<RequestExample>

```bash cURL
curl --request GET \
     --url {{BASE_URL}}/v2/timeseries/{user_id}/hypnogram?start_date={{START_DATE}}&end_date={{END_DATE}} \
     --header 'Accept: application/json' \
     --header 'x-vital-api-key: <API_KEY>'
```

```python Python
from vital.client import Vital
from vital.environment import VitalEnvironment

client = Vital(
  api_key="YOUR_API_KEY",
  environment=VitalEnvironment.SANDBOX
)

data = client.vitals.hypnogram(
    user_id=*,
    start_date="2022-05-01",
    end_date="2022-06-01"
)
```

```javascript Node
import { VitalClient, VitalEnvironment } from '@tryvital/vital-node';
import { VitalsHypnogramRequest } from '@tryvital/vital-node/api/resources/vitals';

const client = new VitalClient({
    apiKey: '<my_api_key>',
    environment: VitalEnvironment.Sandbox,
});

const request: VitalsHypnogramRequest = {
    startDate: "2022-05-01",
    endDate: "2022-06-01"
}

const data = await client.vitals.hypnogram(
    '<my_user_id>',
    request
);
```

```java Java
import com.vital.api.Vital;
import com.vital.api.core.Environment;
import com.vital.api.resources.vitals.requests.VitalsHypnogramRequest;

Vital vital = Vital.builder()
        .apiKey("YOUR_API_KEY")
        .environment(Environment.SANDBOX)
        .build();

VitalsHypnogramRequest request = VitalsHypnogramRequest.builder()
        .startDate("2022-05-01")
        .endDate("2022-06-01")
        .build();

var data = vital.vitals().hypnogram("<user_id>", request);
```

```go Go
import (
  "context"

  vital "github.com/tryVital/vital-go"
  vitalclient "github.com/tryVital/vital-go/client"
)

client := vitalclient.NewClient(
  vitalclient.WithApiKey("<YOUR_API_KEY>"),
  vitalclient.WithBaseURL(vital.Environments.Sandbox),
)

EndDate := "2022-06-01"
request := &vital.VitalsHypnogramRequest{
  StartDate: "2022-05-01",
  EndDate:   &EndDate,
}

response, err := client.Vitals.Hypnogram(context.TODO(), "*", request)
if err != nil {
  return err
}
fmt.Printf("Received data %s\n", response)
```

</RequestExample>

<ResponseExample>

```json Example
[
  {
    "id": null,
    "timestamp": "2022-04-20T13:02:01.123771+00:00",
    "start": "2022-04-20T13:02:01.123771+00:00",
    "end": "2022-04-20T13:03:01.123771+00:00",
    "value": 1,
    "type": null,
    "unit": "stage"
  }
]
```

</ResponseExample>
