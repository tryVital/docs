---
title: "Link Password provider"
openapi: "POST /v2/link/provider/password/{provider}"
---

<RequestExample>

```bash Bash
curl --request POST \
     --url {{BASE_URL}}/v2/link/provider/password/{provider} \
     --header 'Accept: application/json' \
     --header 'Content-Type: application/json' \
     --header 'x-vital-link-token: <VITAL-LINK-TOKEN>' \
     --data '
{
     "username": "test",
     "password": "test"
}
'
```

```js JavaScript
const user = await client.Link.connectProvider(
  linkToken,
  provider,
  username,
  password
);
```

```python Python
user = client.Link.password_provider(link_token, provider, username, password)
```

</RequestExample>

<ResponseExample>

```json Example
{
  "provider": "zwift",
  "connected": true,
  "provider_id": "<provider_id>"
}
```

</ResponseExample>
