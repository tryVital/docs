---
title: "IGG"
openapi: "GET /v2/timeseries/{user_id}/igg"
---

<RequestExample>

```bash cURL
curl --request GET \
     --url {{BASE_URL}}/v2/timeseries/{user_id}/igg?start_date={{START_DATE}}&end_date={{END_DATE}} \
     --header 'Accept: application/json' \
     --header 'x-vital-api-key: <API_KEY>'
```

```python Python
from vital.client import Vital
from vital.environment import VitalEnvironment

client = Vital(
  api_key="YOUR_API_KEY",
  environment=VitalEnvironment.SANDBOX
)

data = client.vitals.igg(
    user_id=*,
    start_date="2022-05-01",
    end_date="2022-06-01"
)
```

```javascript Node
import { VitalClient, VitalEnvironment } from '@tryvital/vital-node';
import { VitalsIggRequest } from '@tryvital/vital-node/api/resources/vitals';

const client = new VitalClient({
    apiKey: '<my_api_key>',
    environment: VitalEnvironment.Sandbox,
});

const request: VitalsIggRequest = {
    startDate: "2022-05-01",
    endDate: "2022-06-01"
}

const data = await client.vitals.igg(
    '<my_user_id>',
    request
);
```

```java Java
import com.vital.api.Vital;
import com.vital.api.core.Environment;
import com.vital.api.resources.vitals.requests.VitalsIggRequest;

Vital vital = Vital.builder()
        .apiKey("YOUR_API_KEY")
        .environment(Environment.SANDBOX)
        .build();

VitalsIggRequest request = VitalsIggRequest.builder()
        .startDate("2022-05-01")
        .endDate("2022-06-01")
        .build();

var data = vital.vitals().igg("<user_id>", request);
```

```go Go
import (
  "context"

  vital "github.com/tryVital/vital-go"
  vitalclient "github.com/tryVital/vital-go/client"
)

client := vitalclient.NewClient(
  vitalclient.WithApiKey("<YOUR_API_KEY>"),
  vitalclient.WithBaseURL(vital.Environments.Sandbox),
)

EndDate := "2022-06-01"
request := &vital.VitalsIggRequest{
  StartDate: "2022-05-01",
  EndDate:   &EndDate,
}

response, err := client.Vitals.Igg(context.TODO(), "*", request)
if err != nil {
  return err
}
fmt.Printf("Received data %s\n", response)
```

</RequestExample>

<ResponseExample>

```json Example
[
  {
    "id": 232,
    "timestamp": "2022-04-20T13:02:01.123778+00:00",
    "value": "70",
    "type": "baha_grass",
    "unit": "FSU"
  }
]
```

</ResponseExample>
