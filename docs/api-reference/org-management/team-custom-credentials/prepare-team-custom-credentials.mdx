---
title: "Prepare Team Custom Credentials"
description: "Prepare and validate custom OAuth credentials before applying them to a team."
openapi: POST /v1/org/{org_id}/prepare_team_custom_credentials/{env}/{region}
---

import OrgManagementAPI from '/snippets/org-management-api.mdx';

<OrgManagementAPI />

<RequestExample>
```bash cURL
curl --request GET \
  --url https://api.management.junction.com/v1/org/{org_id}/prepare_team_custom_credentials/{env}/{region} \
  --header 'X-Vital-Org-Key: <api-key>'
```
</RequestExample>
