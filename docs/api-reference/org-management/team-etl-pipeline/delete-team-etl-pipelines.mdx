---
openapi: delete /v1/org/{org_id}/team_etl_pipelines/{env}/{region}
title: Delete Team ETL Pipelines
description: "Delete org team ETL pipelines via the Junction API. Requires authentication with your team API key."
---

import OrgManagementAPI from '/snippets/org-management-api.mdx';

<OrgManagementAPI />


<RequestExample>
```bash cURL
curl --request DELETE \
  --url https://api.management.junction.com/v1/org/{org_id}/team_etl_pipelines/{env}/{region} \
  --header 'X-Vital-Org-Key: <api-key>'
```
</RequestExample>
