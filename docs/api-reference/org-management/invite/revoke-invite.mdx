---
openapi: delete /v1/org/{org_id}/invite/{invite_id}
title: Revoke Invite
description: "Delete org invite via the Junction API. Requires authentication with your team API key."
---

import OrgManagementAPI from '/snippets/org-management-api.mdx';

<OrgManagementAPI />


<RequestExample>
```bash cURL
curl --request DELETE \
  --url https://api.management.junction.com/v1/org/{org_id}/invite/{invite_id} \
  --header 'X-Vital-Org-Key: <api-key>'
```
</RequestExample>
