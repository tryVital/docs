import { Swagger } from "@theme/swagger";

# Overview

## List of supported providers

<Swagger endpoint={"/providers/"} method={"get"} title={"Get list of providers"} include_client_instantiation>

```bash
curl --request GET \
     --url {{BASE_URL}}/v2/providers/ \
     --header 'Accept: application/json'
     --header 'x-vital-api-key: <API_KEY>' \
'
```

```js
const user = await client.User.create(client_user_id);
```

```python
user = client.User.create(client_user_id)
```

</Swagger>
