import { Swagger } from "@theme/swagger";

# Overview

## Summary

<Swagger endpoint={"/summary/sleep/{user_id}"} method={"get"} title={"Get Sleep stream"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Vitals.heartrate(user_id=*,
    start_date="2020-01-01",
    end_date="2021-01-01"
)
```

</Swagger>

## Stream

<Swagger endpoint={"/timeseries/sleep/{sleep_id}/stream"} method={"get"} title={"Get Sleep stream"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Vitals.heartrate(user_id=*,
        start_date="2020-01-01",
        end_date="2021-01-01"
        )
```

</Swagger>
