import { Swagger } from "@theme/swagger";

# Overview

## Summary

<Swagger endpoint={"/summary/body/{user_id}"} method={"get"} title={"Get body data"}>

```python
from vital import Client

client = Client(client_id, client_secret, "sandbox")

data = client.Body.get(user_id=*,
      start_date="2020-01-01",
      end_date="2021-01-01"
      )
```

</Swagger>

## Raw

<Swagger endpoint={"/summary/body/{user_id}/raw"} method={"get"} title={"Get Raw body Data"}>

```python
from vital import Client

client = Client(client_id, client_secret, "sandbox")

data = client.ProviderSpecific.get(user_id=*,
          provider="oura"
          start_date="2020-01-01",
          end_date="2021-01-01"
          )
```

</Swagger>
