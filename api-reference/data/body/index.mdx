import { Swagger } from "@theme/swagger";

# Overview

## Summary

<Swagger endpoint={"/summary/body/{user_id}"} method={"get"} title={"Get body data"} include_client_instantiation>

```bash
curl --request GET \
     --url {{BASE_URL}}/v2/summary/body/{user_id} \
     --header 'Accept: application/json'
     --header 'x-vital-api-key: <API_KEY>'
```

```python
data = client.Body.get(
    user_id=*,
    start_date={{START_DATE}},
    end_date={{END_DATE}}
)
```

</Swagger>

## Raw

<Swagger endpoint={"/summary/body/{user_id}/raw"} method={"get"} title={"Get Raw body Data"} include_client_instantiation>

```bash
curl --request GET \
     --url {{BASE_URL}}/v2/summary/body/{user_id}/raw \
     --header 'Accept: application/json'
     --header 'x-vital-api-key: <API_KEY>'
```

```python
data = client.Body.get_raw(
    user_id="*",
    start_date={{START_DATE}},
    end_date={{END_DATE}}
)
```

</Swagger>
