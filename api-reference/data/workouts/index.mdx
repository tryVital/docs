import { Swagger } from "@theme/swagger";

# Overview

## Summary

<Swagger endpoint={"/summary/workouts/{user_id}"} method={"get"} title={"Get workouts data"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Workouts.get(user_id=*,
      start_date="2020-01-01",
      end_date="2021-01-01"
      )
```

</Swagger>

## Raw

<Swagger endpoint={"/summary/workouts/{user_id}/raw"} method={"get"} title={"Get Raw workouts Data"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.ProviderSpecific.get(user_id=*,
          provider="oura"
          start_date="2020-01-01",
          end_date="2021-01-01"
          )
```

</Swagger>
