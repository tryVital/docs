import { Swagger } from "@theme/swagger";

# IGE

<Swagger endpoint={"/timeseries/{user_id}/igg"} method={"get"} title={"Get heartrate data"}>

```python
from vital import Client

client = Client(client_id, client_secret, "sandbox")

data = client.Vitals.igg(user_id=*,
        start_date="2020-01-01",
        end_date="2021-01-01"
        )
```

</Swagger>
