import { Swagger } from "@theme/swagger";

# IGE

<Swagger endpoint={"/timeseries/{user_id}/ige"} method={"get"} title={"Get heartrate data"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Vitals.igg(user_id=*,
        start_date="2020-01-01",
        end_date="2021-01-01"
        )
```

</Swagger>
