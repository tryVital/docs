import { Swagger } from "@theme/swagger";

# Overview

## Result

<Swagger version={'v3'} endpoint={"/order/{order_id}/result"} method={"get"} title={"Get order Result"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Order.Result.get("<order_id">)
```

</Swagger>

## Result PDF

<Swagger version={'v3'} endpoint={"/order/{order_id}/result/pdf"} method={"get"} title={"Get order results pdf"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Order.Result.pdf("<order_id">)
```

</Swagger>

## Result metadata

<Swagger version={'v3'} endpoint={"/order/{order_id}/result/metadata"} method={"get"} title={"Get order result metadata"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Order.Result.get_metadata("<order_id">)
```

</Swagger>
