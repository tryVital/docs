import { Swagger } from "@theme/swagger";

# Overview

## Get Results

<Swagger endpoint={"/testkit/orders/{order_id}/results"} method={"get"} title={"Get result"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Testkits.result("<order_id>")
```

</Swagger>

## Get Results Raw

<Swagger endpoint={"/testkit/orders/{order_id}/results/raw"} method={"get"} title={"Get result raw"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Testkits.result("<order_id">)
```

</Swagger>

## Get Result Metadata

<Swagger endpoint={"/testkit/orders/{order_id}/results/metadata"} method={"get"} title={"Get result metadata"}>

```python
from vital import Client

client = Client(api_key, "sandbox")

data = client.Testkits.metadata("<order_id>")
```

</Swagger>
